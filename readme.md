# css website

official website for the computer science society.

```bash
make dev      # start dev server
make build    # build for production
make format   # format code with prettier
make clean    # clean build artifacts and dependencies
```
